# Deployment

The repository uses GitHub webhooks to trigger deployments based on events.

## Webhooks
- Webhook 1: push event -> deploys to the staging environment.
- Webhook 2: release event -> deploys to the production environment.
