services:
  fluxer-bot:
    env_file:
      - /opt/fluxer-bot/.env
  postgres:
    env_file:
      - /opt/fluxer-bot/.env
    environment:
      POSTGRES_USER:
      POSTGRES_PASSWORD:
      POSTGRES_DB:
